/**
 * AI Analysis Type Definitions
 * Activity-specific types extending base AI types
 *
 * Note: Base types (AiAnalysisType, AiReport, etc.) are defined in base.types.ts
 */

// Currently no additional activity-specific types needed.
// This file exists for future activity-level AI type extensions.

// Placeholder export to make this a valid module
export type AiActivityPlaceholder = Record<string, never>;
